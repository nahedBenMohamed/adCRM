{% extends 'baseTeacher.html.twig' %}

{% block title %}Inscription{% endblock %}
{% block stylesheets %}
<style>
    .list-choice {
        display: grid;
    }
    .list-choice label {
        margin-top: -15px;
    }
</style>
{% endblock %}
{% block body %}
    <section class="section">
        <div class="row">
            <div class="col-lg-3"></div>
            <div class="col-lg-6">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Évaluation de Stagiaire "{{ trainee.firstName }} {{ trainee.lastName }}"</h5>

                        <!-- add eval Form -->
                        {{ form_start(evaluation) }}
                        <div class="row mb-3">
                            {{ form_row(evaluation.knowledge,
                                {'attr': {'class': 'form-control list-choice'}}
                            ) }}
                        </div>
                        <div class="row mb-3">
                            {{ form_row(evaluation.skills,
                                {'attr': {'class': 'form-control list-choice'}}
                            ) }}
                        </div>
                        <div class="row mb-3">
                            {{ form_row(evaluation.comments,
                                {'attr': {'class': 'form-control'}}
                            ) }}
                        </div>
                        <div class="row pages-links">
                            <div class="col-sm-12">
                                <button type="submit" class="save-icon float-end">
                                    <img src="{{asset('assets/img/icon/save-disk-solid.svg')}}" width="30">
                                </button>
                            </div>
                        </div>
                        {{ form_end(evaluation) }} <!-- End eval user form -->
                    </div>
                </div>

            </div>
        </div>
    </section>
{% endblock %}